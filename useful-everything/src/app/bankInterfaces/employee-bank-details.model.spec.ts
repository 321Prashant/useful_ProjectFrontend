import { EmployeeBankDetails } from './employee-bank-details.model';

describe('EmployeeBankDetails', () => {
  it('should create an instance', () => {
    expect(new EmployeeBankDetails()).toBeTruthy();
  });
});
